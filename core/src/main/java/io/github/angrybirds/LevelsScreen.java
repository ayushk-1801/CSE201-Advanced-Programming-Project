package io.github.angrybirds;

import com.badlogic.gdx.Screen;

public class LevelsScreen implements Screen {
    @Override
    public void show() {
        // This method is called when the screen becomes the current screen.
    }

    @Override
    public void render(float delta) {
        // This method is called every frame.
    }

    @Override
    public void resize(int width, int height) {
        // This method is called when the screen is resized.
    }

    @Override
    public void pause() {
        // This method is called when the screen is not active.
    }

    @Override
    public void resume() {
        // This method is called when the screen is active.
    }

    @Override
    public void hide() {
        // This method is called when the screen is no longer the current screen.
    }

    @Override
    public void dispose() {
        // This method is called when the screen is removed.
    }
}
